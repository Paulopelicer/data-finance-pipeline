FROM apache/airflow:2.9.1-python3.11

USER root
RUN apt-get update && apt-get install -y --no-install-recommends \
    openjdk-17-jdk-headless \
    libfontconfig1 \
    fonts-dejavu-core \
    && rm -rf /var/lib/apt/lists/*

ENV JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64

USER airflow

COPY requirements.txt /tmp/requirements.txt
COPY z_infra/requirements-docker.txt /tmp/requirements-docker.txt
RUN pip install --no-cache-dir -r /tmp/requirements.txt -r /tmp/requirements-docker.txt

COPY --chown=airflow:root . /opt/airflow/b3_platform
ENV PYTHONPATH="/opt/airflow/b3_platform:${PYTHONPATH}"

RUN mkdir -p /opt/airflow/outputs
