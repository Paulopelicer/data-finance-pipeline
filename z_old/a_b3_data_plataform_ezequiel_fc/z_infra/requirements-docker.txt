# Extra dependencies for Docker containers (Airflow + MinIO storage)
# These are NOT needed for local pipeline execution.

# Orchestration
apache-airflow>=2.9.0

# S3 / MinIO storage
boto3>=1.34.0
s3fs>=2024.2.0
